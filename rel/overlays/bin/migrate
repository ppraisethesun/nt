#!/bin/sh
cd -P -- "$(dirname -- "$0")"
# exec ./nt_task eval NtTask.Release.migrate

exec="./nt_task"
module="NtTask.ReleaseTasks"

echo "running migrations..."
START_APP=false ${exec} eval "${module}.migrate()" || exit 1
echo "migrations finished"
